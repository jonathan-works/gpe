<?xml version="1.0" encoding="UTF-8"?>

<page xmlns="http://jboss.org/schema/seam/pages" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	  xsi:schemaLocation="http://jboss.org/schema/seam/pages http://jboss.org/schema/seam/pages-2.3.xsd">

	<begin-conversation join="true" />

	<param name="order" value="#{eventoList.order}" />
	<param name="restrictionLogicOperator" value="#{eventoList.restrictionLogicOperator}" />

	<param name="evento" value="#{eventoList.entity.evento}" />
	<param name="eventoSuperior" value="#{eventoList.entity.eventoSuperior}" />
	<param name="observacao" value="#{eventoList.entity.observacao}" />
	<param name="status" value="#{eventoList.entity.status}" />
	<param name="ativo" value="#{eventoList.entity.ativo}" />

</page>