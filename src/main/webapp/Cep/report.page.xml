<?xml version="1.0" encoding="UTF-8"?>

<page xmlns="http://jboss.org/schema/seam/pages" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	  xsi:schemaLocation="http://jboss.org/schema/seam/pages http://jboss.org/schema/seam/pages-2.3.xsd">

	<begin-conversation join="true" />

	<param name="order" value="#{cepList.order}" />
	<param name="restrictionLogicOperator" value="#{cepList.restrictionLogicOperator}" />
	
	<param name="numeroCep" value="#{cepList.entity.numeroCep}"/>
	<param name="nomeLogradouro" value="#{cepList.entity.nomeLogradouro}"/>
	<param name="nomeBairro" value="#{cepList.entity.nomeBairro}"/>
	<param name="municipio" value="#{cepList.entity.municipio}"/>
	<param name="complemento" value="#{cepList.entity.complemento}"/>
	<param name="ativo" value="#{cepList.entity.ativo}"/>

</page>