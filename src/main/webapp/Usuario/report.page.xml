<?xml version="1.0" encoding="UTF-8"?>

<page xmlns="http://www.jboss.org/schema/seam/pages"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.jboss.org/schema/seam/pages http://www.jboss.org/schema/seam/pages-2.3.xsd">

	<begin-conversation join="true"/>
   <param name="order" value="#{usuarioGrid.order}"/>

<action execute="#{usuarioGrid.setMaxResults(10000)}"/>

<!-- Parametros do searchField -->
	<param name="nome" value="#{usuarioSearch.nome}"/>		
	<param name="bloqueio" value="#{usuarioSearch.bloqueio}"/>		
	<param name="ativo" value="#{usuarioSearch.ativo}"/>		
	<param name="provisorio" value="#{usuarioSearch.provisorio}"/>		
	<param value="#{usuarioGrid.visibleColumnList}"/>

</page>